## Process this file with automake to produce Makefile.in

##discobindir = $(prefix)/bin
##discobin_DATA = disco.fp

discodir = $(prefix)/bin
disco_DATA = disco.fp

bin_PROGRAMS = disco

disco_SOURCES = disco.c disco.h

disco_LDADD = -lpcap

extra_DIST = disco.fp

